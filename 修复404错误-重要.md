# 🔧 修复 main.jsx 404 错误

## 问题原因

如果看到 `main.jsx:1 Failed to load resource: 404` 错误，说明 GitHub Pages 正在加载**开发版本**的 `index.html`（来自 `main` 分支），而不是**构建版本**（来自 `gh-pages` 分支）。

## ✅ 解决方案

### 步骤 1：检查 GitHub Pages 设置（最重要！）

1. 访问 GitHub 仓库设置：
   ```
   https://github.com/514ctdawn/Star-Greenhouse-100-Survival/settings/pages
   ```

2. 检查 **Source** 设置：
   - ✅ **正确设置**：`Deploy from a branch` → `gh-pages` → `/ (root)`
   - ❌ **错误设置**：`Deploy from a branch` → `main` → `/ (root)` 或 `/docs`

3. 如果设置错误：
   - 选择 `gh-pages` 分支
   - 选择 `/ (root)` 文件夹
   - 点击 **Save**
   - 等待 1-2 分钟让 GitHub Pages 更新

### 步骤 2：清除浏览器缓存

**方法 A：硬刷新（最快）**
- Windows: `Ctrl + Shift + R` 或 `Ctrl + F5`
- Mac: `Cmd + Shift + R`

**方法 B：使用无痕模式**
- Chrome: `Ctrl + Shift + N` (Windows) 或 `Cmd + Shift + N` (Mac)
- 然后访问网站

**方法 C：清除缓存**
1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

### 步骤 3：验证部署

在浏览器中直接访问这些链接，检查是否返回 404：

✅ **主页面**（应该显示 HTML 内容）：
```
https://514ctdawn.github.io/Star-Greenhouse-100-Survival/
```

✅ **JavaScript 文件**（应该下载或显示 JS 代码）：
```
https://514ctdawn.github.io/Star-Greenhouse-100-Survival/assets/index-Cdb7LujM.js
```

✅ **CSS 文件**（应该显示 CSS 代码）：
```
https://514ctdawn.github.io/Star-Greenhouse-100-Survival/assets/index-BZ9h87A7.css
```

如果这些链接都**可以访问**，说明部署是正确的，问题只是浏览器缓存。

### 步骤 4：检查浏览器控制台

1. 按 `F12` 打开开发者工具
2. 点击 **Network**（网络）标签
3. 刷新页面（`F5`）
4. 查看哪些文件返回 404

**如果看到：**
- ❌ `/src/main.jsx` → 说明加载的是开发版本（GitHub Pages 设置错误）
- ✅ `/Star-Greenhouse-100-Survival/assets/index-xxx.js` → 说明加载的是构建版本（正确）

## 🎯 快速检查清单

- [ ] GitHub Pages Source 设置为 `gh-pages` 分支
- [ ] GitHub Pages Folder 设置为 `/ (root)`
- [ ] 已清除浏览器缓存
- [ ] 已等待 1-5 分钟让 GitHub Pages 更新
- [ ] 使用无痕模式测试

## 📝 如果问题仍然存在

1. **重新部署**：
   ```bash
   npm run deploy
   ```

2. **检查 gh-pages 分支内容**：
   ```bash
   git fetch origin gh-pages
   git show origin/gh-pages:index.html
   ```
   应该看到引用 `/Star-Greenhouse-100-Survival/assets/index-xxx.js`，而不是 `/src/main.jsx`

3. **联系支持**：如果以上步骤都不行，可能需要检查 GitHub Pages 的构建日志。

---

**记住：最重要的是确保 GitHub Pages 设置指向 `gh-pages` 分支，而不是 `main` 分支！**
